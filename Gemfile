source "https://rubygems.org"

# This gem bundles the exact Jekyll version and plugins that GitHub Pages uses
gem "github-pages", "~> 232", group: :jekyll_plugins

# Ruby 3+ often needs webrick explicitly for `jekyll serve`
gem "webrick", "~> 1.8"
